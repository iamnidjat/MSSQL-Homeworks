@Task1

Создайте таблицу Authors со следущими стоблцами:

 - Id [первичный ключ, наращение];
 - Имя [не может быть NULL];
 - Фамилия;
 - Отчество;
 - Дата рождения;
 - Почта [уникальное, не может быть NULL]
 - Мобильный телефон [уникальное].


@Task2

Создайте таблицу Books со следущими стоблцами:

 - Id [первичный ключ, наращение];
 - Название [не может быть NULL];
 - ФИО автора [не может быть NULL];
 - Название издателя [не может быть NULL];
 - Количество книг [не может быть NULL];
 - Цена одной книги [не может быть NULL];
 - Скидка [не может быть меньше 0 и больше 1].

Подумайте, какая нормальная(ые) форма(ы) здесь нарушена(ы), и нарушены ли вообще, и напишите ответ в коменнтирии .sql файла.

@Task3<:

Для @Task2 удалите стоблец Скидка.

@Task4:10

Для @Task1 и @Task2 заданий добавьте ограничения через CONSTRAINT.

@Task5

Удалите все созданные таблицы.


! Отправляемый скрипт для всех заданий не должен содержать закоментированного кода.
